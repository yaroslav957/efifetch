[package]
name = "efifetch"
authors = ["Yaroslav Lobachev"]
categories = ["embedded", "uefi"]
repository = "https://github.com/yaroslav957/efifetch"
keywords = ["uefi", "fetch", "efi", "osdev"]
version = "0.2.0"
edition = "2024"
license = "MIT"

[profile.dev]
debug = true
panic = "abort"

[profile.release]
lto = "thin"
strip = "symbols"
panic = "abort"

[dependencies.uefi]
version = "*"
features = ["panic_handler", "global_allocator"]
